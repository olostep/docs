{
  "name": "olostep-docs",
  "private": true,
  "engines": {
    "node": ">=18.0.0 <25.0.0"
  },
  "scripts": {
    "start:dev": "mint dev --port 3000",
    "dev": "mint dev --port 3000",
    "dev:fast": "mint dev --port 3000 --disable-openapi --no-open",
    "dev:port": "mint dev --port 3333",
    "lint": "mint broken-links",
    "lint:openapi": "mint openapi-check"
  },
  "devDependencies": {
    "mint": "^4.2.257"
  }
}
